import SwiftUI

struct ThisWeekView: View {
    var body: some View {
        Text("This Week — coming in Phase 7")
            .foregroundStyle(.secondary)
            .navigationTitle("This Week")
    }
}
